# Правильная стратегия работы с ветками

## ❌ Что НЕЛЬЗЯ делать

Вы **НЕ МОЖЕТЕ** напрямую пушить изменения в ветки `upstream/dev` или `upstream/main` из вашего форка. Эти ветки доступны только для чтения (read-only).

## ✅ Правильный подход

Вам нужно создать **свою собственную** ветку `dev` в вашем форке, а затем создать Pull Request.

## 📊 Две стратегии

### Стратегия 1: Создать dev от вашей main (проще)

**Подходит, если:** Вы хотите просто загрузить свой проект в dev ветку владельца.

**Шаги:**
1. Убедитесь, что вы на ветке `main` (ваша основная ветка)
2. **Branch → New branch**
3. Имя: `dev`
4. Создать от: `main` (ваша ветка, не upstream!)
5. Опубликовать ветку `dev` в ваш форк
6. Создать PR: ваш форк `dev` → оригинал `dev`

**Результат:** Ваш проект будет в PR для ветки dev

### Стратегия 2: Создать dev от upstream/dev (лучше для синхронизации)

**Подходит, если:** Вы хотите начать с актуальной версии dev ветки владельца и добавить туда свои изменения.

**Шаги:**
1. **Branch → New branch**
2. Имя: `dev`
3. Создать от: `upstream/dev` (ветка владельца)
4. Теперь у вас будет чистая копия dev ветки владельца
5. Добавьте свои изменения (через merge вашей main или копирование файлов)
6. Закоммитьте изменения
7. Опубликовать ветку `dev` в ваш форк
8. Создать PR: ваш форк `dev` → оригинал `dev`

**Результат:** Ваши изменения будут добавлены к актуальной версии dev ветки

## 🎯 Рекомендация для вашей ситуации

Используйте **Стратегию 1** (проще и быстрее):

1. Ваш проект уже в `main`
2. Создайте `dev` от `main` - все файлы скопируются автоматически
3. Опубликуйте `dev`
4. Создайте PR

## 📝 Визуальная схема

```
Ваш форк (mrfrunze/christmas-recipe-site)
├── main (ваш проект) ✅
└── dev (создать от main) ✅
    └── PR → Оригинал (ufyhlexi/christmas-recipe-site)
        └── dev (принимающая ветка) ✅

Оригинал (ufyhlexi/christmas-recipe-site) - ТОЛЬКО ДЛЯ ЧТЕНИЯ
├── main (ветка владельца) - upstream/main ❌ (нельзя пушить)
└── dev (ветка владельца) - upstream/dev ❌ (нельзя пушить)
```

## ⚠️ Важно понимать

- `upstream/dev` и `upstream/main` - это ветки **оригинального репозитория**
- Вы можете их **читать** (fetch, pull), но **не можете** в них пушить
- Вы создаете **свою** ветку `dev` в **вашем форке**
- Затем создаете Pull Request, чтобы владелец принял ваши изменения

## 🔄 Если нужно синхронизироваться с upstream/dev

Если вы хотите, чтобы ваша `dev` была актуальной с `upstream/dev`:

1. Создайте `dev` от `upstream/dev` (Стратегия 2)
2. Затем можете смержить вашу `main` в `dev`:
   - Переключитесь на `dev`
   - **Branch → Merge into current branch...**
   - Выберите `main`
   - Завершите merge

Это объединит изменения владельца и ваши изменения.

