# Инструкция: Создание Pull Request в ветку dev через GitHub Desktop

## 🎯 Цель
Загрузить свой проект в ветку `dev` оригинального репозитория `ufyhlexi/christmas-recipe-site`, чтобы владелец мог потом слить `dev` в `main`.

## 📋 Ваша ситуация
- ✅ У вас есть форк репозитория
- ✅ Ваш проект уже в ветке `main` вашего форка
- ❌ У вас НЕТ ветки `dev` в вашем форке
- ✅ В оригинальном репозитории есть ветка `dev`

## 📋 Пошаговая инструкция

### Шаг 1: Убедитесь, что ваш проект закоммичен в main

1. Откройте GitHub Desktop
2. Убедитесь, что вы находитесь на ветке `main` (проверьте название ветки вверху)
3. Проверьте раздел **Changes** (внизу слева)
4. Если есть незакоммиченные изменения:
   - Введите commit message
   - Нажмите **Commit to main**
5. Если есть неопубликованные коммиты, нажмите **Push origin**

### Шаг 2: Настройка upstream (оригинального репозитория)

1. В GitHub Desktop выберите ваш форк репозитория
2. Меню: **Repository → Repository settings...**
3. Перейдите на вкладку **Remote**
4. Проверьте, есть ли уже remote с именем `upstream`
   - Если есть - можете пропустить следующие шаги
   - Если нет - продолжайте
5. Нажмите **Add remote**
6. Заполните:
   - **Name**: `upstream`
   - **Remote URL**: `https://github.com/ufyhlexi/christmas-recipe-site.git`
7. Нажмите **Add**
8. Закройте настройки

### Шаг 3: Синхронизация с оригинальным репозиторием

1. Меню: **Repository → Fetch origin** (или нажмите `Ctrl+Shift+F`)
   - Это загрузит информацию о всех ветках из оригинала, включая `dev`
2. Меню: **Branch → Pull from upstream...** (опционально)
   - Выберите ветку `main` из `upstream`
   - Это синхронизирует вашу `main` с оригиналом
   - ⚠️ **Внимание**: Если у вас есть изменения в `main`, которые отличаются от оригинала, могут возникнуть конфликты

### Шаг 4: Создание ветки dev в вашем форке

**Это самый важный шаг!**

1. Убедитесь, что вы на ветке `main` (проверьте название ветки вверху GitHub Desktop)
2. Меню: **Branch → New branch** (или нажмите `Ctrl+Shift+N`)
3. В поле "Name" введите: `dev`
4. В поле "Create branch based on" выберите `main` (ваша текущая ветка)
5. Нажмите **Create branch**
6. GitHub Desktop автоматически переключится на ветку `dev`
   - Убедитесь, что вверху теперь написано "dev" (а не "main")

### Шаг 5: Проверка содержимого ветки dev

После создания ветки `dev` от `main`, все ваши файлы уже должны быть в ветке `dev` (так как вы создали её от `main`, где находится ваш проект).

1. Убедитесь, что вы на ветке `dev` (проверьте название ветки вверху)
2. Проверьте раздел **Changes** (внизу слева)
   - Если там есть изменения - значит есть незакоммиченные файлы
   - Если пусто - значит все уже закоммичено

**Если есть незакоммиченные изменения:**
3. Просмотрите изменения, чтобы убедиться, что все нужные файлы включены
4. Введите commit message (например: "Добавлен проект в ветку dev")
5. Нажмите **Commit to dev** (внизу слева)

**Если изменений нет:**
- Это нормально! Ваши файлы уже были в `main`, и теперь они в `dev`
- Просто продолжайте к следующему шагу

### Шаг 6: Публикация ветки dev

1. Убедитесь, что вы на ветке `dev` (вверху должно быть написано "dev")
2. Нажмите **Publish branch** (вверху справа, рядом с названием ветки)
   - Или: **Branch → Publish branch**
3. GitHub Desktop загрузит вашу ветку `dev` в ваш форк на GitHub
4. Дождитесь завершения публикации (вы увидите сообщение об успехе)

### Шаг 7: Создание Pull Request

**Способ 1: Через GitHub Desktop (рекомендуется)**

1. После публикации ветки `dev` в GitHub Desktop появится уведомление с кнопкой **Create Pull Request**
2. Нажмите на эту кнопку - откроется браузер с формой создания PR
3. Перейдите к шагу 8 для настройки PR

**Способ 2: Через веб-интерфейс GitHub**

1. Меню: **Repository → View on GitHub** (или **Branch → Show in GitHub**)
2. На странице репозитория GitHub вы увидите желтую полосу с предложением создать Pull Request для ветки `dev`
3. Нажмите **Compare & pull request**
4. Перейдите к шагу 8 для настройки PR

### Шаг 8: Настройка Pull Request

1. **Важно!** Убедитесь, что настройки PR правильные:
   - **Base repository**: `ufyhlexi/christmas-recipe-site`
   - **Base branch**: `dev` ⬅️ **Это важно!** Должна быть ветка `dev`, а не `main`
   - **Compare**: `mrfrunze/christmas-recipe-site` → `dev`

2. Заполните информацию о Pull Request:
   - **Title**: Описание ваших изменений (например: "Добавление проекта в ветку dev")
   - **Description**: Подробное описание того, что вы добавили

3. Нажмите **Create pull request**

### Шаг 9: Ожидание ответа владельца

После создания Pull Request:
- Владелец репозитория получит уведомление
- Он сможет просмотреть ваши изменения
- При необходимости он может запросить изменения (request changes)
- После одобрения он сольет ваш PR в ветку `dev`
- Потом он сам сольет `dev` → `main` в своем репозитории

## 🔍 Проверка перед созданием PR

Убедитесь, что:
- ✅ Вы находитесь на ветке `dev` (а не `main`)
- ✅ Все ваши файлы закоммичены
- ✅ Ветка `dev` опубликована в ваш форк
- ✅ В Pull Request выбран base branch = `dev` (не `main`!)

## 🐛 Возможные проблемы и решения

### Проблема: Не вижу опцию "Pull from upstream"

**Решение:**
1. Убедитесь, что вы добавили `upstream` remote (Шаг 1)
2. Меню: **Repository → Fetch origin**
3. Попробуйте снова

### Проблема: Ветка dev не видна

**Решение:**
1. Убедитесь, что вы выполнили **Fetch origin**
2. Создайте ветку `dev` вручную в вашем форке
3. Затем создайте Pull Request

### Проблема: Конфликты при синхронизации

**Решение:**
1. Если есть конфликты, GitHub Desktop покажет их
2. Разрешите конфликты вручную
3. Завершите merge
4. Продолжите с созданием ветки `dev`

### Проблема: Не могу создать Pull Request в ветку dev

**Решение:**
1. Убедитесь, что ветка `dev` существует в оригинальном репозитории
2. Если нет - создайте её сначала в оригинальном репозитории (через веб-интерфейс GitHub)
3. Или создайте PR в `main`, но в описании укажите, что хотите влить в `dev`

## 📝 Альтернативный способ (через веб-интерфейс)

Если GitHub Desktop не работает как ожидается:

1. Откройте ваш форк на GitHub: `https://github.com/mrfrunze/christmas-recipe-site`
2. Убедитесь, что ваша ветка `dev` опубликована
3. Нажмите **Pull requests** → **New pull request**
4. Настройте:
   - Base: `ufyhlexi/christmas-recipe-site:dev`
   - Compare: `mrfrunze/christmas-recipe-site:dev`
5. Создайте PR

## ✅ Финальная проверка

После создания Pull Request проверьте:

1. Перейдите на страницу PR: `https://github.com/ufyhlexi/christmas-recipe-site/pulls`
2. Убедитесь, что:
   - PR направлен в ветку `dev` (не `main`)
   - Все ваши файлы видны в изменениях
   - Нет конфликтов (если есть - разрешите их)

## 🎉 Готово!

После создания Pull Request владелец репозитория сможет:
1. Просмотреть ваши изменения
2. Обсудить их с вами (через комментарии)
3. Слить ваши изменения в ветку `dev`
4. Потом слить `dev` → `main`

Ваш проект будет в оригинальном репозитории! 🚀

